<!-- skill-suite pointer — appended by the suite ZIP; do not remove. -->
# Skill suite

This project uses the DMBG skill suite (idea → maquette → build) for planning work. Everything the suite needs lives in `planning/`. **Before any planning-related work, read `planning/AGENTS.md`** (Claude: `planning/CLAUDE.md`) and follow it. Product code lives outside `planning/`; the suite's build skillset is the only part that writes there.
