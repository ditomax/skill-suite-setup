<!-- skill-suite pointer — static; never changes on updates. -->
# planning/

This folder holds the planning work of this project, done with the skill suite. The suite itself lives in `suite/`, the customer's constraints in `profile/`. **Read `suite/AGENTS.md`** (Claude: `suite/CLAUDE.md`) and follow it — it names the installed skillsets, the dispatch rule and the update rule.
