# customers/

One folder per customer: `<name>/customer.yaml` + `<name>/profile/*.md`. Gitignored — customer material never enters the public repo. Alternatively keep a customer's folder inside the customer project and point `setup.py` at it with `--customer-dir <path>`.
