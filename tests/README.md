# Smoke tests

One log per platform: `smoke-<platform>.md` (cowork, chatgpt-codex, vibe-cli). Each log records: ZIP built from which customer and versions · unzip → open → `start` → did the right Director greet with version and profile? · one stage run · update path (move to `_archive/`, unzip) with work folders untouched · findings. The riskiest assumption of the delivery model is "the agent reads `planning/AGENTS.md` / `planning/CLAUDE.md` when the folder is opened" — these logs are what turns it into a fact.

| Platform | Date | Customer / build | Result | Log |
| --- | --- | --- | --- | --- |
| cowork | — | — | pending | — |
| chatgpt-codex | — | — | pending | — |
| vibe-cli | — | — | pending | — |
